Pillow ~= 9.3.0
pyinstaller~=5.7.0
PySide6 ~= 6.4.0
texture2ddecoder ~= 1.0.4
simpleaudio~=1.0.4
# For audio, we need PyOgg from source, since we need our own fork to fix a library loading bug on MacOS
git+https://github.com/Didero/PyOgg.git#egg=PyOgg
sfyi-fsb5
